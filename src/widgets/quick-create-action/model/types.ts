/** The action a user picks in the quick-create menu. */
export type QuickCreateChoice = 'appointment' | 'timeOff'

/** The overlay used to present quick-create actions. */
export type QuickCreatePresentation = 'modal' | 'drawer'
