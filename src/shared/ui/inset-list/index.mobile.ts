// Mobile (Ionic) public API for the inset-list — deliberately kept out of the
// shared/ui barrel (index.ts), which pulls in Nuxt-UI components. Import this
// only from the Ionic build target.
export { default as InsetList } from './ui-mobile/InsetList.vue'
