// Mobile (Ionic) public API for the list-picker-modal — deliberately kept out
// of the shared/ui barrel (index.ts), which pulls in Nuxt-UI components. Import
// this only from the Ionic build target.
export { default as ListPickerModal } from './ui-mobile/ListPickerModal.vue'
