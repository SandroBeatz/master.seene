{
  "extends": "./tsconfig.app.json",

  // Override to include only test files and clear exclusions.
  // Application code imported in tests is automatically included via module resolution,
  // but ambient global declarations (auto-imported composables/components) and module
  // shims (e.g. side-effect CSS imports) must be listed explicitly since `include`
  // overrides rather than merges with the extended config.
  "include": ["src/**/__tests__/*", "src/**/*.d.ts", "env.d.ts", "auto-imports.d.ts", "components.d.ts"],
  "exclude": [],

  "compilerOptions": {
    // Vitest runs in a different environment than the application code.
    // Adjust lib and types accordingly.
    "lib": [],
    "types": ["node", "jsdom"],

    // `vue-tsc --build` produces a .tsbuildinfo file for incremental type-checking.
    // Specified here to keep it out of the root directory.
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.vitest.tsbuildinfo"
  }
}
